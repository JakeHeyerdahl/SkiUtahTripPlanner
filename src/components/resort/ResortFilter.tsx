// TODO: Resort filter sidebar/bar
// Filters: Region (radio), Skill Level (checkbox), Min Acreage (slider), Pass (checkbox: Epic/Ikon)
// On change: updates URL search params, re-renders resort grid
export default function ResortFilter() {
  return <div>Resort filter placeholder</div>;
}
