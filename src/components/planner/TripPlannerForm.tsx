// TODO: Multi-step trip planner form
// Step 1: Where & When — region picker (map or buttons), date range picker, guest count (adults/kids)
// Step 2: Preferences — skill level (beginner/intermediate/advanced), budget tier
// Step 3: Results — curated cards for resort, lodging, activities, dining, gear, ski school
// Uses URL state / query params to persist between steps
export default function TripPlannerForm() {
  return <div>Trip planner form placeholder</div>;
}
